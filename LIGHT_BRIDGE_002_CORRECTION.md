# Correction record — LIGHT Bridge 002

The first status label for `EXPERIMENT_LIGHT_BRIDGE_002.md` used `EXECUTED`. That label was too strong: the construction was derived analytically and was not run as a computational experiment.

Correct status for the result is:

`DERIVED / FINITE COUNTEREXAMPLE`

The result itself is unchanged: a declared locality predicate can differ while graph connectivity remains identical, and scalar path summaries can fail to preserve behavior when a transition rule depends on intermediate organization.

No computational execution is claimed by this record.
