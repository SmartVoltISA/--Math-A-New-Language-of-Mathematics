# Ω-Math — Mathematical Extension Status v1.1

**Purpose:** canonical status register after closure of the two-field coupled-vector mathematical gate.

## Closed conditional gates

| Gate | Status |
|---|---|
| Positive quadratic conservation → metric-skew generator | Conditional theorem |
| Local antisymmetric lattice propagation | Mathematical model + executed audit |
| Isotropic first-order curl-sector selection | Conditional selection result + executed audit |
| Two-field transverse conserved coupling | Conditional theorem/model class + executed audit |
| Continuum dispersion and speed relation | Conditional derivation |

## New boundary audit

`ASSUMPTION_RELAXATION_MATRIX.md` removes baseline assumptions one at a time and records which conclusions cease to be selected.

The normalized two-field result is therefore a **joint selection result**, not a theorem from relation alone.

## Dispersion result

For the normalized continuum system,

`∂t E = ω curl B`

`∂t B = −ω curl E`,

transverse modes satisfy

`Ω² = ω²|k|²`.

Thus the mathematical phase/group speed magnitude is `|ω|`, while its absolute physical calibration remains open.

## Evidence discipline

- Derivation = algebraic consequence of declared assumptions.
- Executed = computationally tested in Ω-Lab.
- Supported = numerical result agrees with the declared derivation/test.
- Counterexample = demonstrates failure after an assumption is removed.
- Hypothesis = interpretation not established by the mathematical layer.

## Explicit non-claims

This status does not establish Maxwell equations, electromagnetism, light, Lorentz invariance, gauge symmetry, physical units, physical time, physical energy, or any physical ontology.

## Next gate

The next mathematical task is not to add more interpretation. It is to test whether the current selection survives progressively weaker assumptions and whether a minimal variational/Hamiltonian formulation adds genuinely new constraints or merely repackages the same conserved structure.

**Status:** v1.1 extension layer synchronized.
