# Ω-Math — Mathematical Extension Status v1.1

**Purpose:** Canonical index for mathematical structures added after the v0.9 minimal typed-language closure.

## Status discipline

These extensions do not reopen the v0.9 language core. They formalize additional mathematical model classes and results transferred from Ω-Lab.

## Registered extensions

| Extension | Status |
|---|---|
| Conservation and operator selection | Conditional theorem within declared linear class |
| Spectral relational dynamics | Mathematical classification |
| Local relational propagation | Mathematical model class |
| Dissipation vs conservation | Mathematical classification |
| Relational circulation | Structural mathematical vocabulary |
| Vector relational operators | Conditional selection result |
| Scale and time | Boundary theorem for dimensionless autonomous class |
| Two-field coupled vector system | Conditional theorem/model class |
| Two-field coupling audit | Executed numerical consistency audit |

## Evidence classes

- **Definition:** introduced by declaration.
- **Derivation:** follows algebraically from declared assumptions.
- **Executed:** computationally tested in source research repository or local audit.
- **Supported:** agrees with the declared mathematical tests.
- **Counterexample:** demonstrates failure outside the stated class.
- **Hypothesis:** proposed but not established.

## Canonical separation

`Ω-Math` defines and organizes mathematical objects.

`Ω-Lab` executes experiments and audits.

`Physical interpretation` remains a separate layer.

No model result is promoted to a physical fact merely by being represented in Ω-Math.

## Main chain

`distinction → relation → operator → conservation/constraint → spectrum → mode → propagation → field structure`

This chain is conditional on the mathematical structures explicitly introduced at each arrow.

## Closed gate result: two-field system

Within the declared linear, first-order, translation-invariant, rotationally covariant transverse vector-field class, positive quadratic conservation reduces the field-space generator to a metric-skew 2×2 form. After a positive field-space normalization, the minimal nontrivial sector is

`∂t E = ω curl B`

`∂t B = -ω curl E`.

The constraints select the curl/antisymmetric sector and opposite-sign cross-coupling up to normalization and convention. They do not select a physical interpretation, physical units, Lorentz invariance, gauge structure, sources, quantization, or electromagnetism.

A 1000-trial numerical audit confirms the conservation residual and real-eigenvalue components remain at floating-point roundoff scale for the constructed positive metrics.

## Next open mathematical gate

Test whether the same selection survives when the restrictions are relaxed one at a time: variable constitutive metric, anisotropy, nonlocality, higher spatial order, and nonlinear coupling. Record exactly which assumptions are necessary rather than merely sufficient.
